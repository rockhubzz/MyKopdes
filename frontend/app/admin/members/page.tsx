'use client';

// Admin-scoped alias of the shared Members screen (see ./items/page.tsx).
import OwnerMembersPage from '@/app/owner/members/page';

export default function AdminMembersPage() {
  return <OwnerMembersPage />;
}
