'use client';

// Admin-scoped alias of the shared Reports screen (see ./items/page.tsx).
import OwnerReportsPage from '@/app/owner/reports/page';

export default function AdminReportsPage() {
  return <OwnerReportsPage />;
}
