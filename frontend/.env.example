# Base URL the browser uses to reach the Laravel API.
# Relative (same-origin): the browser calls <origin>/api, which Nginx
# proxies to the backend. This works from any host/IP, not just localhost.
NEXT_PUBLIC_API_BASE_URL=/api
