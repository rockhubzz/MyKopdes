# dependencies
/node_modules

# production build output
/.next/

# local env (use .env.example as the template; compose injects real values)
/.env
/.env*.local

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor noise
.DS_Store
*.pem
